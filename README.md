# pdf2video 🎥📄

Превратите ваши статические PDF-презентации в динамичные, кинематографичные видеоролики. Идеально подходит для создания контента в TikTok, YouTube Shorts, Instagram Reels и образовательных видео.

## ✨ Ключевые особенности

- **Кинематографичный Зум (Zoom Drift):** Умный эффект "вдоха-выдоха" — плавное приближение и возврат к исходному масштабу ровно к моменту перехода.
- **Бесшовные переходы:** Поддержка эффектов `xfade` (fade, wipe, slide, pixelize и др.).
- **Музыкальное сопровождение:** Автоматическая синхронизация длительности видео с вашим аудио-треком.
- **Пресеты форматов:**
  - `16:9` — YouTube / Презентации.
  - `9:16` — TikTok / Shorts / Reels.
  - `4:5` — Instagram.
- **Высокое качество:** Рендеринг PDF через `ImageDPI` с настраиваемым качеством (до 300+ DPI).
- **Производительность:** Параллельный рендеринг страниц с использованием всех ядер вашего процессора.
- **Умная автоматизация:** Автоматический подбор последних файлов из папок `input/pdf` и `input/audio`.

## 🛠 Требования

Для работы приложения необходимы:
1. **Go** (версия 1.23 или выше)
2. **FFmpeg** (должен быть доступен в системном PATH)

## 🚀 Быстрый старт

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/ivlev/pdf2video.git
   cd pdf2video
   ```

2. Положите ваш PDF в папку `input/pdf/`, а фоновую музыку — в `input/audio/`.

3. Запустите конвертацию:
   ```bash
   go run cmd/pdf2video/main.go
   ```

## ⚙️ Настройка (Флаги)

Приложение поддерживает множество параметров для тонкой настройки:

| Флаг | Описание | По умолчанию |
|------|----------|--------------|
| `-preset` | Формат видео (`16:9`, `9:16`, `4:5`) | - |
| `-duration` | Общая длительность видео (сек) | 0 (авто) |
| `-page-duration` | Длительность одного слайда (если duration=0) | 0.3 |
| `-audio` | Путь к аудио-файлу | последняя в input/audio |
| `-zoom-mode` | Тип движения (`center`, `random`, `top-left` и др.) | `center` |
| `-zoom-speed` | Скорость зума | `0.001` |
| `-transition` | Тип перехода (`fade`, `wipeleft`, `slideup`, `pixelize`) | `fade` |
| `-fade` | Длительность эффекта перехода (сек) | `0.5` |
| `-dpi` | Качество рендеринга PDF | `300` |
| `-workers` | Количество потоков обработки | runtime.NumCPU |

### Примеры использования:

**Для TikTok (9:16) со случайным зумом:**
```bash
go run cmd/pdf2video/main.go -preset 9:16 -zoom-mode random -transition slideup
```

**Высокое качество для YouTube (4K-ready):**
```bash
go run cmd/pdf2video/main.go -preset 16:9 -dpi 600 -zoom-speed 0.0005
```

## 🏗 Архитектура

Проект построен на модульной объектно-ориентированной архитектуре:
- `internal/pdf`: Рендеринг и работа с документами.
- `internal/video`: Взаимодействие с FFmpeg.
- `internal/effects`: Генерация визуальных фильтров.
- `internal/engine`: Оркестратор процесса.

Подробности в [project_meta/ARCHITECTURE.md](project_meta/ARCHITECTURE.md).

## 📊 Планы развития

Следите за прогрессом в [project_meta/ROADMAP.md](project_meta/ROADMAP.md). Основные цели:
- Аппаратное ускорение (GPU encoding).
- Поддержка водяных знаков.
- Оптимизация через Memory Pipes.

---
Разработано с ❤️ для создателей контента.
